# Copy this file to `.env.local` (git-ignored) and fill in real values.

# Public site origin (used for absolute URLs and OG tags).
NEXT_PUBLIC_SITE_URL=https://apex.example.com

# Optional: override contact email without rebuilding (Next.js reads at build time).
# NEXT_PUBLIC_CONTACT_EMAIL=apex@yourdomain.ac.kr